import { debugCode } from "./debugCode.js";
import { documentCode } from "./documentCode.js";
import { optimizeCode } from "./optimizeCode.js";

export const commands = {
  documentCode,
  optimizeCode,
  debugCode,
};
